<form method="post" action="{{ path('app_check_attendance_delete', {'id': check_attendance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ check_attendance.id) }}">
    <button class="btn">Delete</button>
</form>
