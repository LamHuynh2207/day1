{% extends 'base.html.twig' %}

{% block title %}CheckAttendance{% endblock %}

{% block body %}
    <h1>CheckAttendance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ check_attendance.id }}</td>
            </tr>
            <tr>
                <th>AttendanceDate</th>
                <td>{{ check_attendance.attendanceDate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_check_attendance_index') }}">back to list</a>

    <a href="{{ path('app_check_attendance_edit', {'id': check_attendance.id}) }}">edit</a>

    {{ include('check_attendance/_delete_form.html.twig') }}
{% endblock %}
